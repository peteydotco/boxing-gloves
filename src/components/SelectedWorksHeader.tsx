export function SelectedWorksHeader() {
  // Now rendered inside ProjectCardsGrid — this component is kept as a no-op
  // so the import in App.tsx doesn't break. Remove from App.tsx when ready.
  return null
}
